# Placeholder compose file.
# Run `node update.js` from the repo root to fetch Plane's self-host files.
services:
  placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'Plane compose will be generated by update.js' && sleep 3600"]
    restart: unless-stopped
